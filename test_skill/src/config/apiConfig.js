const API_BASE_URL = "https://your-api-url.com";

export const API_ENDPOINTS = {
  uploadFile: `${API_BASE_URL}/upload`,
  calculateTotal: `${API_BASE_URL}/calculate`,
  updateService: `${API_BASE_URL}/service/update`,
};
